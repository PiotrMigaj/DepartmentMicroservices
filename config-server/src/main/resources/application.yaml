server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PiotrMigaj/config-server-repo.git
          default-label: main
          clone-on-start: true

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/




